fn main() {
    // Wiring is added in a later milestone; the library + tests carry the work
    // until the pipeline binary is assembled.
    println!("cme_md_connector: see `cargo test` for the implemented modules so far.");
}
